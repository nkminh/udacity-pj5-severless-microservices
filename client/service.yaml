apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    app: udagram-frontend
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: udagram-frontend
  type: LoadBalancer